ReadingAssetへのご登録、ありがとうございます。
このメールは、ご登録いただいたメールアドレスの確認用に自動送信されたものです。

以下のリンクをクリックして、ユーザー登録を完了させてください。

<%= "#{Thread.current[:base_url]}/users/#{@user.id}/verify?vt=#{@user.verification_token}" %>

ReadingAsset
